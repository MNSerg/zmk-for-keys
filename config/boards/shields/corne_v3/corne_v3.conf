# Shared shield defaults — features enabled per-side
# 5 ms is the usual ZMK default; 20 ms feels sluggish on USB/BLE
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_KEYBOARD_NAME="Corne"
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
# Idle at 10s → RGB AUTO_OFF_IDLE; deep sleep after 60 min inactivity
CONFIG_ZMK_IDLE_TIMEOUT=10000
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000
# CFG layer &soft_off — wake with nice!nano RESET on each half
CONFIG_ZMK_PM_SOFT_OFF=y
